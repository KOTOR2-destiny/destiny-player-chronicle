<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
>
<meta
    http-equiv="Cache-Control"
    content="no-cache, no-store, must-revalidate"
>
<title>Destiny Player Chronicle</title>

<style>
*{
    box-sizing:border-box;
}

html,
body{
    margin:0;
    min-height:100%;
    background:
        radial-gradient(
            circle at top,
            #073d50 0%,
            #02141d 42%,
            #00070b 100%
        );
    color:#d9fbff;
    font-family:Arial,Helvetica,sans-serif;
}

body{
    min-height:100vh;
    padding:22px;
}

button,
input,
select,
textarea{
    font:inherit;
}

.chronicle-shell{
    width:min(1500px,96vw);
    margin:0 auto;
    border:1px solid #40dfff;
    background:rgba(0,10,16,.94);
    box-shadow:
        0 0 45px rgba(49,217,255,.2),
        inset 0 0 25px rgba(24,138,170,.07);
}

.topbar{
    display:flex;
    justify-content:space-between;
    align-items:center;
    gap:18px;
    padding:18px 20px;
    border-bottom:1px solid #286d7d;
    background:
        linear-gradient(
            180deg,
            rgba(6,42,55,.95),
            rgba(2,24,34,.95)
        );
}

.title{
    color:#9af1ff;
    font-size:23px;
    font-weight:bold;
    letter-spacing:4px;
}

.subtitle{
    margin-top:5px;
    color:#6999a5;
    font-size:10px;
    letter-spacing:2px;
}

.top-actions{
    display:flex;
    gap:8px;
    flex-wrap:wrap;
    justify-content:flex-end;
}

.action-button{
    min-height:38px;
    padding:0 14px;
    display:inline-flex;
    align-items:center;
    justify-content:center;
    text-align:center;
    border:1px solid #4edfff;
    background:rgba(6,43,56,.95);
    color:#d5fbff;
    cursor:pointer;
    font-size:9px;
    font-weight:bold;
    letter-spacing:1.4px;
}

.action-button.good{
    border-color:#6fe39a;
    color:#caffda;
    background:rgba(9,51,31,.93);
}

.action-button.purple{
    border-color:#9a7bff;
    color:#e4dcff;
    background:rgba(34,24,62,.93);
}

.main{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr 390px;
}

.column{
    min-width:0;
    padding:14px;
}

.column + .column{
    border-left:1px solid #245f6f;
}

.section-title{
    margin-bottom:10px;
    color:#72e5fb;
    font-size:11px;
    font-weight:bold;
    letter-spacing:2px;
}

.search{
    width:100%;
    height:39px;
    margin-bottom:10px;
    padding:0 10px;
    border:1px solid #327b8f;
    background:#031d28;
    color:#d7fbff;
    outline:none;
}

.npc-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}

.npc-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}

.npc-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}

.npc-name{
    font-size:12px;
    font-weight:bold;
    letter-spacing:1px;
}

.npc-meta{
    margin-top:4px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}

.empty{
    padding:30px 8px;
    text-align:center;
    color:#658f98;
    font-size:9px;
    letter-spacing:1.4px;
}

.dossier{
    min-height:650px;
}

.dossier-placeholder{
    min-height:620px;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:30px;
    color:#678f99;
    text-align:center;
    font-size:11px;
    letter-spacing:2px;
    line-height:1.7;
}

.dossier-header{
    padding-bottom:12px;
    margin-bottom:12px;
    border-bottom:1px solid #2c6d7c;
}

.dossier-name{
    color:#b6f8ff;
    font-size:22px;
    font-weight:bold;
    letter-spacing:2px;
}

.dossier-tags{
    margin-top:8px;
    display:flex;
    flex-wrap:wrap;
    gap:6px;
}

.tag{
    padding:5px 7px;
    border:1px solid #397d8e;
    background:rgba(7,41,52,.9);
    color:#aeeef8;
    font-size:8px;
    letter-spacing:1px;
}

.dossier-block{
    margin-top:12px;
    padding:11px;
    border:1px solid #245b69;
    background:rgba(2,24,33,.82);
}

.block-label{
    margin-bottom:7px;
    color:#70d8ea;
    font-size:9px;
    font-weight:bold;
    letter-spacing:1.5px;
}

.block-text{
    white-space:pre-wrap;
    color:#d2f5fa;
    font-size:12px;
    line-height:1.6;
}

.author-row{
    display:grid;
    grid-template-columns:1fr 150px;
    gap:8px;
    margin-bottom:8px;
}

.input,
.select,
.textarea{
    width:100%;
    border:1px solid #33788a;
    background:#031e29;
    color:#d9fbff;
    padding:9px;
    outline:none;
}

.input,
.select{
    height:38px;
}

.textarea{
    min-height:110px;
    resize:vertical;
}

.submit-button{
    width:100%;
    min-height:40px;
    margin-top:8px;
    border:1px solid #69e598;
    background:rgba(7,52,30,.94);
    color:#caffd8;
    cursor:pointer;
    font-weight:bold;
    letter-spacing:1.4px;
}

.submission-list{
    margin-top:12px;
    max-height:400px;
    overflow-y:auto;
}

.submission-card{
    margin-bottom:8px;
    padding:9px;
    border:1px solid #355f6b;
    background:rgba(2,24,33,.9);
}

.submission-top{
    display:flex;
    justify-content:space-between;
    gap:8px;
    margin-bottom:6px;
}

.submission-kind{
    color:#9deeff;
    font-size:8px;
    font-weight:bold;
    letter-spacing:1px;
}

.submission-author{
    color:#819ea5;
    font-size:8px;
    letter-spacing:.8px;
}

.submission-text{
    color:#d9f8fc;
    font-size:11px;
    line-height:1.45;
    white-space:pre-wrap;
}

.delete-submission{
    margin-top:7px;
    border:1px solid #8f555d;
    background:rgba(55,14,18,.9);
    color:#ffc3c8;
    cursor:pointer;
    font-size:8px;
    padding:5px 7px;
}

.import-status{
    padding:9px 14px;
    border-top:1px solid #245f6f;
    color:#75aeba;
    font-size:9px;
    letter-spacing:1.3px;
}

input[type="file"]{
    display:none;
}


.chronicle-nav{
    display:flex;
    gap:7px;
    padding:9px 14px;
    border-bottom:1px solid #245f6f;
    background:rgba(1,17,24,.96);
}

.nav-button{
    min-height:34px;
    padding:0 12px;
    border:1px solid #316f7f;
    background:#031e29;
    color:#83ddeb;
    cursor:pointer;
    font-size:8px;
    font-weight:bold;
    letter-spacing:1.3px;
}

.nav-button.active{
    border-color:#70e8ff;
    color:#d9fbff;
    box-shadow:0 0 10px rgba(72,220,255,.2);
}

.nav-button.locked{
    opacity:.38;
    cursor:default;
}

.journal-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr 390px;
}

.journal-sidebar,
.journal-reader,
.journal-editor{
    min-width:0;
    padding:14px;
}

.journal-reader,
.journal-editor{
    border-left:1px solid #245f6f;
}

.journal-list{
    margin-top:10px;
    max-height:575px;
    overflow-y:auto;
}

.journal-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}

.journal-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}

.journal-card-title{
    font-size:11px;
    font-weight:bold;
    letter-spacing:1px;
}

.journal-card-meta{
    margin-top:5px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}

.journal-entry{
    padding:18px;
    border:1px solid #2a6675;
    background:
        linear-gradient(
            180deg,
            rgba(4,35,46,.92),
            rgba(2,20,28,.92)
        );
    box-shadow:inset 0 0 20px rgba(42,180,210,.05);
}

.journal-entry-title{
    color:#b7f7ff;
    font-size:21px;
    font-weight:bold;
    letter-spacing:2px;
}

.journal-entry-byline{
    margin-top:8px;
    color:#72c8d7;
    font-size:9px;
    letter-spacing:1.2px;
}

.journal-entry-text{
    margin-top:20px;
    color:#d8f6fa;
    white-space:pre-wrap;
    line-height:1.75;
    font-size:13px;
}

.journal-field{
    margin-bottom:8px;
}

.journal-meta-grid{
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:8px;
    margin-bottom:8px;
}

.journal-textarea{
    min-height:330px;
}

.journal-delete{
    width:100%;
    min-height:36px;
    margin-top:8px;
    border:1px solid #8f555d;
    background:rgba(55,14,18,.9);
    color:#ffc3c8;
    cursor:pointer;
    font-size:8px;
    font-weight:bold;
    letter-spacing:1.2px;
}


.location-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr;
}
.location-sidebar,
.location-reader{
    min-width:0;
    padding:14px;
}
.location-reader{
    border-left:1px solid #245f6f;
}
.location-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}
.location-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}
.location-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}
.location-name{
    font-size:12px;
    font-weight:bold;
    letter-spacing:1px;
}
.location-meta{
    margin-top:4px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}


.faction-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr;
}
.faction-sidebar,
.faction-reader{
    min-width:0;
    padding:14px;
}
.faction-reader{
    border-left:1px solid #245f6f;
}
.faction-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}
.faction-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}
.faction-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}
.faction-name{
    font-size:12px;
    font-weight:bold;
    letter-spacing:1px;
}
.faction-meta{
    margin-top:4px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}


.mission-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr;
}
.mission-sidebar,
.mission-reader{
    min-width:0;
    padding:14px;
}
.mission-reader{
    border-left:1px solid #245f6f;
}
.mission-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}
.mission-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}
.mission-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}
.mission-name{
    font-size:12px;
    font-weight:bold;
    letter-spacing:1px;
}
.mission-meta{
    margin-top:4px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}


.session-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr;
}
.session-sidebar,
.session-reader{
    min-width:0;
    padding:14px;
}
.session-reader{
    border-left:1px solid #245f6f;
}
.session-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}
.session-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}
.session-card.selected{
    border-color:#79edff;
    box-shadow:0 0 10px rgba(73,220,255,.27);
}
.session-name{
    font-size:12px;
    font-weight:bold;
    letter-spacing:1px;
}
.session-meta{
    margin-top:4px;
    color:#6fa4af;
    font-size:8px;
    letter-spacing:.7px;
}


.discovery-section{
    min-height:720px;
    display:grid;
    grid-template-columns:330px 1fr;
}
.discovery-sidebar,
.discovery-reader{
    min-width:0;
    padding:14px;
}
.discovery-reader{
    border-left:1px solid #245f6f;
}
.discovery-list{
    max-height:650px;
    overflow-y:auto;
    padding-right:4px;
}
.discovery-card{
    width:100%;
    margin-bottom:7px;
    padding:10px;
    border:1px solid #2c6878;
    background:rgba(3,30,40,.95);
    color:#d8fbff;
    text-align:left;
    cursor:pointer;
}
.discovery-card.selected{
    border-color: